import styled from "@emotion/styled";

export const VoteStartText= styled.div(({theme})=>({
	color: theme.color.darkblue,
	fontSize:theme.fontSize.sm,
	width: "100%",
	display: "flex",
	justifyContent: "center",
}))